name: My first workflow

on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name:   Print a welcome message
        run: echo "Welcome to our first github actions workflow"

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Lost repo files
        run: ls -la
