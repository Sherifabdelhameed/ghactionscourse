on:
  workflow_dispatch:
    inputs:
      enviroument:
        description: "Choose Enviroument"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy application
        run: echo 'Deploying Application'
