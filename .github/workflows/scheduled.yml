name:  Schedule workflow
on:
  schedule:
    - cron: "0 0 * * *"
    - cron: "30 12 * * *"
jobs:
  daily-task:
    runs-on: ubuntu-latest
    steps:
      - name: Print Message
        run: |
          echo "Running the scheduled task"
          sleep 3s
          echo "Done"
        
